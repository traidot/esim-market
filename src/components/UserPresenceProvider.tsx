/**
 * User Presence Provider
 * Component để quản lý user presence (online/offline status) globally
 * Gọi useUserPresence hook để connect WebSocket và update status
 */

'use client'


/**
 * User Presence Provider Component
 * - Gọi useUserPresence hook để connect WebSocket
 * - Update status khi user login/logout
 * - Không render gì, chỉ để gọi hook
 */
export function UserPresenceProvider() {
  // Không render gì
  return null
}

